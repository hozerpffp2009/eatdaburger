<hi>Burgers!</hi>

<h2>burgers that havent been devoured</h2>

<ul>
    {{#each burgers}}
        {{#if devour}}
            {{> burgers/burger-block devour=true}}
        {{/if}}
    {{/each}}
</ul>

<h2>burgers that have been devoured</h2>

<ul>
    
    {{#each burgers}}
        {{#unless devour}}
            {{> burgers/burger-block devour=false}}
        {{/unless}}
    {{/each}}
</ul>

<h1>Add your favorite burger</h1>
<form class="create-form">
    <div class="form-group">
        <label for="bu">Burger name:</label>
        <input type="text" id="bu" name="name">
    </div>

    <div class="form-group">
        <label for="newBurger">new burger</label><br>
        <input type="radio" name="newBurger" value="1" checked> New Creation! <br>
    </div>

    <button type="submit">Add new creation</button>
</form>